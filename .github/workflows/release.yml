name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set version
        id: version
        run: echo "tag=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"

      - name: Build binaries
        uses: crazy-max/ghaction-xgo@v3
        with:
          xgo_version: v0.36.0
          go_version: 1.25
          dest: dist
          pkg: ./cmd/witc
          prefix: witc
          targets: linux/amd64,linux/arm64,darwin/amd64,darwin/arm64,windows/amd64
          ldflags: "-s -w"

      - name: Package artifacts
        run: |
          for target in linux-amd64 linux-arm64 darwin-amd64 darwin-arm64 windows-amd64; do
            os="${target%%-*}"
            arch="${target##*-}"
            if [ "$os" = "windows" ]; then
              cp "dist/witc-${target}.exe" "witc-${{ steps.version.outputs.tag }}-${os}-${arch}.exe"
              zip "witc-${{ steps.version.outputs.tag }}-${os}-${arch}.zip" "witc-${{ steps.version.outputs.tag }}-${os}-${arch}.exe"
              rm "witc-${{ steps.version.outputs.tag }}-${os}-${arch}.exe"
            else
              cp "dist/witc-${target}" "witc-${{ steps.version.outputs.tag }}-${os}-${arch}"
              chmod +x "witc-${{ steps.version.outputs.tag }}-${os}-${arch}"
              tar czf "witc-${{ steps.version.outputs.tag }}-${os}-${arch}.tar.gz" "witc-${{ steps.version.outputs.tag }}-${os}-${arch}"
              rm "witc-${{ steps.version.outputs.tag }}-${os}-${arch}"
            fi
          done
          ls -la witc-*.zip witc-*.tar.gz

      - name: SHA256 checksums
        run: |
          sha256sum witc-*.zip witc-*.tar.gz > dist/checksums.sha256
          cat dist/checksums.sha256

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            witc-*.zip
            witc-*.tar.gz
            dist/checksums.sha256
